use super::AES;

impl AES {
    // fn inv_sub_bytes(&self, prev_state: State) -> State {
    //     State::default()
    // }

    // fn inv_shift_rows(&self, prev_state: State) -> State {
    //     State::default()
    // }

    // fn inv_mix_columns(&self, prev_state: State) -> State {
    //     State::default()
    // }
}
