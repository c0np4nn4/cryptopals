use crypto_utils::base64_enc;

fn main() {
    // println!("{}", base64_enc("beeeef".to_string()).unwrap());

    // println!("{}", base64_enc("beee".to_string()).unwrap());

    println!("{}", base64_enc("be".to_string()).unwrap());
}
